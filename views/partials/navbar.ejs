<!-- Top navigation-->
<nav class="navbar navbar-expand-lg navbar-light bg-light border-bottom">
    <div class="container-fluid">
        <!-- Sidebar Toggle Button (Optional) -->
        <!-- <button class="btn btn-primary me-2" id="sidebarToggle"><i class="bi bi-list"></i></button> -->

        <span class="navbar-brand mb-0 h1">Doctor Appointment System</span>

        <!-- Navbar Toggler for small screens -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Collapsible content -->
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <!-- Welcome Message (Pushed to the right) -->
            <span class="navbar-text ms-auto me-3">
                <i class="bi bi-person-circle me-1"></i> Welcome, <%= username %>!
            </span>

            <!-- User Dropdown -->
            <ul class="navbar-nav mt-2 mt-lg-0">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" id="navbarDropdown" href="#" role="button" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><%= locals.username ? username : 'User' %></a>
                    <div class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href="/settings">Settings</a>
                        <div class="dropdown-divider"></div>
                        <form action="/auth/logout" method="POST" style="display: inline;" id="logout-form">
                            <button type="button" class="dropdown-item" 
                                    onclick="confirmAction('log out of your account', 'logout-form', '#6c757d', 'Yes, log out')">
                                Logout
                            </button>
                        </form>
                    </div>
                </li>
            </ul>
        </div>
    </div>
</nav>
<!-- End Top navigation--> 